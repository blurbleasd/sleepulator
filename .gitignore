.DS_Store
.playwright-cli/
icon.svg.png
proxy/.wrangler/

# dependencies & build output (reproducible via `npm install` + `npm run build`)
node_modules/
dist/

# transient Vite config temp files
vite.config.js.timestamp-*

# local editor / agent settings
.claude/

# one-off migration & refactor scripts (kept on disk, out of version control)
*.cjs
ios-spike.html

# Xcode ignores
xcuserdata/
*.xcuserstate
test_math
